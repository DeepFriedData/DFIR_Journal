<Window x:Class="DFIRNode.Views.RegisterNodeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Register Node" Height="580" Width="480"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0d1117">

    <Window.Resources>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#8b949e"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
        </Style>
        <Style x:Key="FieldBox" TargetType="TextBox">
            <Setter Property="Background" Value="#0d1117"/>
            <Setter Property="Foreground" Value="#e6edf3"/>
            <Setter Property="BorderBrush" Value="#30363d"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="CaretBrush" Value="#58a6ff"/>
        </Style>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#1f6feb"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="SecondaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#21262d"/>
            <Setter Property="Foreground" Value="#e6edf3"/>
            <Setter Property="BorderBrush" Value="#30363d"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="0,10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Register Node" FontSize="20" FontWeight="SemiBold"
                       Foreground="#e6edf3" FontFamily="Segoe UI" Margin="0,0,0,6"/>
            <TextBlock TextWrapping="Wrap" FontFamily="Segoe UI" FontSize="12"
                       Foreground="#8b949e"
                       Text="Generate a registration packet to submit to your Domain Registrar. The packet will be exported as a JSON file for transfer via USB or secure file exchange."/>
        </StackPanel>

        <!-- Device Info Banner -->
        <Border Grid.Row="1" Background="#161b22" BorderBrush="#30363d"
                BorderThickness="1" CornerRadius="6" Padding="14,10" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="DEVICE FINGERPRINT" Foreground="#8b949e"
                           FontSize="10" FontWeight="SemiBold" FontFamily="Segoe UI"
                           Margin="0,0,0,4"/>
                <TextBlock x:Name="FingerprintText" Foreground="#58a6ff"
                           FontFamily="Consolas" FontSize="11" TextWrapping="Wrap"/>
                <TextBlock x:Name="HostnameText" Foreground="#8b949e"
                           FontFamily="Segoe UI" FontSize="11" Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

              <!-- Form -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="NODE NAME" Style="{StaticResource FieldLabel}"/>
            <TextBox x:Name="NodeNameBox" Style="{StaticResource FieldBox}"/>

            <TextBlock Text="YOUR FULL NAME" Style="{StaticResource FieldLabel}"/>
            <TextBox x:Name="UserNameBox" Style="{StaticResource FieldBox}"/>

            <TextBlock Text="YOUR EMAIL" Style="{StaticResource FieldLabel}"/>
            <TextBox x:Name="UserEmailBox" Style="{StaticResource FieldBox}"/>

            <TextBlock x:Name="ErrorText" Foreground="#f85149"
                       FontSize="12" FontFamily="Segoe UI"
                       Visibility="Collapsed" Margin="0,0,0,8"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Cancel"
                    Style="{StaticResource SecondaryBtn}"
                    Click="Cancel_Click"/>
            <Button Grid.Column="2" Content="Generate &amp; Export Packet"
                    Style="{StaticResource PrimaryBtn}"
                    Click="Generate_Click"/>
        </Grid>
    </Grid>
</Window>